# DHCP_Simulation
A C Socket Server/Client UDP DHCP simulation.

Requires an updated `GCC` installation, and that's all!

Build Instructions:
* `cd` into directory
* `make all`
* `./server PORT`
* `./client PORT`
